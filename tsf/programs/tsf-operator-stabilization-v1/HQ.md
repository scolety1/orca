# TSF Operator Stabilization HQ

**Mission ID:** `tsf-operator-stabilization-v1`
**Project:** TSF_ORCA
**Base:** `tsf/main` @ `2613be403cd12ed27f2953e9fade6521ebb0fb79` (clean, current-accepted at mission creation)
**Created:** 2026-09-03

**TWO ACTIVE WORKTREES/BRANCHES exist for this mission as of the Adoption Gate below. Read this whole section before touching either.**

1. **`tsf-operator-stabilization-v1`** (`C:/Users/codex-agent/orca/workspaces/TSF_ORCA/tsf-operator-stabilization-v1`) — the ADOPTION CANDIDATE. **FROZEN at `640cfc64c53ff82ecbf2b07fdc217e86a8224228`** — do not add commits here; any further work happens in the hardening lane below instead. This is the exact commit a future adoption should fast-forward `tsf/main` to, once the Safe Adoption Gate genuinely reads `SAFE_NOW` (see "Adoption Gate attempt" below for why it doesn't yet).
2. **`tsf-operator-hardening-v2`** (`C:/Users/codex-agent/orca/workspaces/TSF_ORCA/tsf-operator-hardening-v2`) — a follow-up hardening lane, branched FROM the frozen candidate commit above, for continued safe bug-fixing/hardening while adoption is blocked. See "Hardening lane" below for its own state. **Whether this lane's commits become part of a later V2 adoption, or the frozen V1 candidate above is adopted as-is with the hardening lane's fixes folded in afterward, is an open decision for whoever adopts — not made autonomously.**

**Phase:** `READY_FOR_ADOPTION_WITH_DISCLOSED_NONBLOCKING_DEBT` (the frozen candidate) — four bounded implementation waves plus a real-project/live-runtime final review wave complete, checkpointed as nine local commits on `tsf-operator-stabilization-v1` (nothing adopted, merged, or pushed). **15 of 16 tracked real bugs are READY_FOR_ADOPTION, independently verified** (several rounds each; every round that found a real defect fixed it and was re-verified — see `bug-ledger.json` per-bug for the full history). The 16th (BUG-07) is honestly `PARTIALLY_READY / ORCA_CORE_DEPENDENCY` — its TSF-owned half is done and verified, its remaining half requires an Orca-core change outside this mission's authority (a concrete, scoped proposal is recorded in its own ledger entry, Orca core itself untouched — and now also a standalone doc, see the hardening lane). BUG-17 stays `DEFERRED / NOT_APPLICABLE_TO_CURRENT_CHAT_CAPABILITY`, an explicit prior operator decision, preserved unchanged. **This session's own full account is in "Final Review Wave" below — read that section, "Adoption Gate attempt", "Hardening lane", and the current `bug-ledger.json` before anything else; the "Wave 1"/"Wave 2" sections below are historical and pre-date the ledger corrections, Wave 3, and final review.**

## Adoption Gate attempt (2026-09-03) — BLOCKED, candidate frozen

Before this attempt, ACCEPT-02 (isolated worktree creation) was the one remaining unverified acceptance item. Closed with real, end-to-end evidence: TSF's real worktree auto-provisioning (`createOrcaWorktree`, wrapping the real `orca worktree create` CLI) was exercised twice against the real, already-running Orca backend, targeting a safe disposable local repo (`quoteloop`, one of this repo's own `tsf/pilots/` projects) with no other active mission/worktree on it — confirmed genuine isolation, zero write into the base checkout, and correct id/path reporting back to the caller. No defect found; both test worktrees fully removed afterward. See ACCEPT-02's own ledger entry for full detail. This produced the checkpoint at `640cfc64c53ff82ecbf2b07fdc217e86a8224228` — **the frozen candidate**.

Then the real, already-implemented Safe Adoption Gate (`GET /api/update-safety`, `domain/update-safety.mjs`'s `classifyUpdateSafety`) was run against the live production TSF instance (read-only, never mutated):

```
{"state":"TIM_REQUIRED","reason":"1 project(s) have an open Needs You question -- resolve those before updating","blockingProjectIds":["niners-war-room"]}
```

**Adoption did not proceed.** Per this mission's own charter, `TIM_REQUIRED`/`WAIT_FOR_ACTIVE_WORK`/an unresolved Needs You anywhere in the fleet halts adoption outright — this is a real, live, currently-true blocker (`niners-war-room` genuinely has an open Needs You question on the live instance as of this check), not a candidate defect. **Before attempting adoption again: re-run `GET /api/update-safety` fresh (this result is time-sensitive and will be stale by the time anyone acts on it) — do not trust this cached result.** If it reads `SAFE_NOW`, follow "Post-adoption plan" below using the frozen candidate HEAD above (or a later one, if the hardening lane's fixes have been folded in by then — see "Hardening lane").

## Hardening lane (`tsf-operator-hardening-v2`)

Opened because adoption was blocked and further safe, bounded TSF-layer work remained valuable to do while waiting, per explicit instruction. Branched from the frozen candidate commit above; the frozen worktree itself was never touched again after that point. See this branch's own commit history (`git log --oneline tsf-operator-stabilization-v1..tsf-operator-hardening-v2`, from either worktree) for the exact diff. Summary as of the last update to this file:

- **BUG-07**: a dedicated investigation re-confirmed (not just re-asserted) no additional TSF-side mitigation exists, and produced `tsf/docs/reference/bug-07-orca-core-notification-followup.md`, a standalone, implementation-ready Orca-core change proposal (exact files/lines, code sketch, gating reuse, test plan, migration risk) for a future properly-scoped Orca-core wave. Orca core itself was never touched.
- **Mission-management UX / Needs You audit**: a read-only investigation found and this lane fixed two real gaps — HomePage's "Needs you" tile was counting attention-reasons, not distinct projects (now `countDistinctNeedsYouProjects`); FleetPage's schedule cards showed the raw internal `projectId` instead of the real `displayName` (now looked up from the loaded portfolio, honest fallback to the raw id in a tooltip if genuinely not found). Also confirmed and disclosed, not fixed: `resolveNeedsYou` remains domain-layer-only with zero HTTP route wiring it up anywhere — an operator can raise a Needs You question but never actually resolve one through any real product surface today. This is a real, pre-existing gap, not new debt from this lane; per this mission's own no-speculative-infrastructure precedent (see BUG-17's history), a real product decision about whether/how to build a resolve UI is left open, not built unreviewed.
- **Operator-state adversarial testing**: new `test/operator-state-adversarial.test.mjs` (9 tests) covering concurrent start/pause races, a PAUSED run carrying an open Needs You question reading consistently across every surface, deep-link query-string safety, cross-project isolation under concurrent load, malformed/corrupted state-file resilience, a stale-revision abandon-stalled-wave race, fleet-removal never orphaning an open Needs You question, and rapid cross-project interleaving. New `ui/src/lib/global-run-status.test.ts` additions cover WAITING-vs-WAITING (paused vs. lock-held) reason-text distinctness, honest null heartbeats, and multiple simultaneous real projects never being dropped/merged in the global indicator.
- **Test-suite reliability**: the two known pre-existing Windows-contention flakes (`test/keep-going-autonomy-proof.test.mjs`, `test/health-repair-io.test.mjs`) had their stall-timeout tolerance increased, confirmed passing reliably in isolation. Disclosed honestly, not claimed fully fixed: this machine hosts several other unrelated Claude Code sessions concurrently (confirmed via `ListAgents`) — genuine external multi-session contention can still occasionally exceed even the increased threshold. See the `shared-machine-multi-session-contention` memory for the full context; this is not a logic defect in either test.
- **Post-adoption smoke automation**: new `tsf/scripts/post-adoption-smoke.mjs` — a read-only, safe-to-run-anytime CLI (`node scripts/post-adoption-smoke.mjs --base-url <url>`) wrapping this repo's own already-adopted `verifyLiveRuntime()` plus additional cross-surface/Evidence/deep-link checks against whatever real project state genuinely exists, with the handful of genuinely unautomatable checks (WebView2, visual rendering, a real non-stub dispatch) explicitly listed as MANUAL rather than silently skipped. Exit code reflects pass/fail. Tested against both a healthy scratch instance (idle and with a real active run) and a deliberately unreachable one.

Remaining priorities not yet started in this lane at the time of this update: further global-execution-indicator UI-level hardening beyond the pure-function tests already added, and a final independent-verification/checkpoint cycle for this lane's own most recent commits — check this lane's own git log and the session's final report for exactly where it left off.

**Ledger correction (2026-09-03, prompted by an explicit operator check, Wave 1):** BUG-08's original title bundled negation-classification correctness (done, verified) with scoped one-time approval consumption (never started) -- marking the whole bug READY_FOR_ADOPTION was status inflation against ACCEPT-07's full criterion. Split: BUG-08 now covers classification correctness only (READY_FOR_ADOPTION, accurate); the approval-consumption mechanism became BUG-17, later resolved by explicit operator decision (see Wave 3 below): `DEFERRED / NOT_APPLICABLE_TO_CURRENT_CHAT_CAPABILITY`, preserved as a mandatory prerequisite the moment Planner Chat gains real consequential execution authority. BUG-05 (Health Repair state loss) was root-caused in wave 2 but found to need a genuine new durable-operation backend subsystem — built and independently verified (3 rounds, 2 real defects found and fixed) in Wave 3, now READY_FOR_ADOPTION.

**Ledger-sync gap found and corrected (final review wave):** BUG-02, BUG-04, and BUG-09 were actually implemented and committed in Wave 3 part 1 (`67a8d4d483`) but their ledger entries were never updated at the time — found and corrected this session per this mission's own "never mark complete without a synced ledger" rule, no new code needed for that correction itself (each subsequently gained its own independent verification and is now READY_FOR_ADOPTION).

This file is the durable next-session startup source for this mission, matching this repo's own established PROJECT_HQ.md / M-wave-doc convention. Any session picking up this worktree should read this file and `bug-ledger.json` first.

## Charter

Orca owns execution; TSF owns intent and consequences. This mission owns **TSF product stabilization only**. It does **not** own Dataset Research Engine development and does **not** own Niners War Room — those are independently owned by other TSF_ORCA missions/chats using other worktrees, and this mission must never modify, pause, clean, reset, merge, or otherwise interfere with either lane.

## Provider split (conserve capacity, don't self-verify)

- **Claude / PLANNER_DEEP** — planning, decomposition, supervision, reconciliation, gap analysis (this session's own role).
- **Codex** — bounded implementation workers where appropriate.
- **Independent verifier** — substantive verification before READY_FOR_ADOPTION (a separate identity/context from whichever provider implemented the fix, per this repo's own established convention — see `tsf/programs/daily-driver-autonomy-v1/state.json`'s wave 12/16b findings on why SELF-verification misses real defects).
- **Orca** — durable execution/runtime.

## Reconciliation findings (2026-09-03)

- `tsf/main` was clean at `2613be403c` when this worktree was cut.
- **A real, prior stalled repair run already exists covering much of this exact ground — preserved as evidence, not touched:**
  - Keep Going run `keep-going-tsf-orca-1787691592122` (project `tsf-orca`), created 2026-08-25, most recent activity 2026-09-03T01:23:55Z.
  - **State: `STALLED`** — last checkpoint `WAVE_STALLED silent for 1822185ms` (~30 min silent at last observation).
  - Its own independent verification pass (2026-09-03T00:53:25Z) found **8 unsatisfied acceptance criteria** against its original goal (the same bug list: Health Repair nav bug, Projects nav-position bug, composer too small, Planner Chat can't initiate governed work, plus test/build/lint/verifier/zero-delta/READY_FOR_ADOPTION gates) — i.e. as of that check, nothing in it was yet acceptable.
  - Its worktree, `C:/Users/codex-agent/orca/workspaces/TSF_ORCA/command-tsf-orca-1788395222325`, is **dirty (uncommitted)** and contains partial, unverified work touching several of the same bugs this mission now owns: `tsf/ui/src/lib/chat-composer-height.ts(+test)`, `tsf/ui/src/lib/projects-navigation-state.ts(+test)`, `tsf/ui/src/lib/health-repair-activity.ts(+test)`, plus modified `chat-responder.mjs`, `data-store.mjs`, `http-server.mjs`, `PlannerChatPanel.tsx`, `ProjectHealthRepairCard.tsx`, `HealthRepairCenterPage.tsx`, `ProjectsPage.tsx`.
  - **Disposition: left exactly as found.** Not reset, not cleaned, not merged, not reused. Future waves of this mission may *read* it for reference/salvage candidates, but must independently reproduce and verify anything before relying on it — it already failed one independent verification pass.
- Other worktrees present and explicitly out of scope for this mission (each belongs to a different lane/mission): `tsf-command-authority-repair-v1` (this session's own prior, separate Command Authority repair, already at READY_FOR_ADOPTION, untouched by this mission), `tsf-command-workflow-repair-v1`, `tsf-parallel-improvement-v1`, `tsf-autonomy-program-v1`, `featherstar`, `nixie`.

## Golden-path acceptance (the real bar — passing unit/integration tests alone does not stabilize TSF)

Tracked as `ACCEPT-*` entries in `bug-ledger.json`:
1. User requests a new governed mission through Planner Chat.
2. TSF creates a separate isolated mission/worktree.
3. Exact mission appears globally as RUNNING.
4. User navigates elsewhere and returns without losing context.
5. A test mission can enter STALLED and be recovered through the UI/Planner.
6. Planner Chat reports the exact same canonical state as Flight Recorder/Work.
7. Needs You authorization is requested only when appropriate, understands negation, and consumes scoped approval once.
8. Finished work reaches READY_FOR_ADOPTION clearly.
9. Post-adoption live smoke tests verify the actual operator workflow.

## Bug ledger states

`REPORTED → REPRODUCED → FIXING → VERIFYING → READY_FOR_ADOPTION → ADOPTED → LIVE_VERIFIED`

Nothing is marked DONE before live verification after adoption. See `bug-ledger.json` for the live table.

## Priority order

**P0:** one canonical durable runtime state shared by Planner Chat/Work/Keep Going/Flight Recorder/Home/Fleet/notifications; reliable stall/recovery controls; authorization/negation correctness; global execution visibility.
**Then:** mission-creation UX; navigation/context preservation; Needs You / Ready for Review workflow; notifications; forms/composer polish; jargon reduction.

## Wave 1 (2026-09-03): canonical state + P0 operator-control + authorization negation

Scope: BUG-14 (canonical state -- root), BUG-11 (non-interactive CONTINUE), BUG-12 (deep-link to exact run), BUG-13 (Planner Chat blindness), BUG-16 (Flight Recorder/Evidence gap), BUG-08 (authorization negation -- the real, reproduced `and`-clause-splitting gap; the fuller scoped-approval-consumption ask is deliberately deferred to a following wave, disclosed in bug-ledger.json's BUG-08 entry).

Deliberately deferred to the next wave (not started): BUG-15 (full stall-recovery UX: heartbeat/worker/provider display -- worker/provider identity is a genuine, disclosed pre-existing gap, not yet wired to any live orchestration binding), BUG-05 (Health Repair surface -- separate investigation needed), BUG-01/02/03/06/07/09/10 (P1, out of this wave's P0 scope), a persistent always-visible global state indicator (BUG-14's own remaining gap), and a real chat-triggered Resume/Retry/Replan/Abandon action (chat can *describe* the exact recovery control now, per BUG-13's fix; *triggering* it from chat is new dispatch-bridge-shaped surface area not yet built).

All 6 bugs: reproduced first (failing test or live repro before the fix), fixed, unit-tested (target module + wiring layer), full suite re-run clean (986/988 server tests pass -- the 1 failure is `keep-going-autonomy-proof.test.mjs`, a pre-existing timing flake confirmed via isolated reruns before AND after this wave's changes, unrelated; 43/43 ui tests, `tsc -b --noEmit` clean, `vite build` clean), then live-verified against the real running standalone server + built UI in a real Chrome tab using the safe local FIXTURE project (real Keep Going run started/paused, cross-checked consistent across Keep Going/Flight Recorder/Planner Chat, including one genuine live Claude Code/Sonnet 5 planner call proving BUG-13's capsule grounding end to end) -- zero console errors throughout. Full detail per bug in bug-ledger.json.

Independent verification (separate agent context each round, per the provider-split rule below): 3 rounds total. Round 1 cleared BUG-11/12/13/16 outright and found 2 real defects: BUG-08's own `and`-fix had introduced a new false-negative on future-tense directive fragments ("...and will deploy after that"), and BUG-14's HomePage.tsx had a genuine untested React key collision (a project can appear in both `work.blocked` and `work.needsYou` at once). Both fixed; round 2 (fresh agent context) confirmed both fixes correct and, while re-checking BUG-08, found a THIRD real pre-existing gap unrelated to the "and" fix: `PROHIBITION_MARKERS` had no idiom awareness, so "whether ... or not"/"no matter" (meaning "regardless") were misread as real negations. Fixed; round 3 (fresh agent context) confirmed that fix too, plus a final full-suite sanity check on the whole wave. All 6 bugs are now READY_FOR_ADOPTION -- see bug-ledger.json per-bug for full detail. This 3-round pattern is itself the strongest evidence yet in this mission for HQ's own provider-split rule: self-review alone would have shipped 2 real classification defects.

Zero Orca-core delta: every change is inside tsf/. `tsf/ui` node_modules/dist were installed/built locally to run typecheck/build verification -- both gitignored, no tracked-file impact.

**Checkpointed:** commit `4aafc1b1fc6f8c63846884ba059beb2ceda01dee` (local only, not pushed) -- see the ledger-correction note above for the BUG-08/BUG-17 split made after this checkpoint.

## Wave 2 (2026-09-03): BUG-15 + persistent global execution visibility, BUG-05 root-caused and deferred

Scope: BUG-15 (stalled-run recovery -- real stalled-wave detail: dispatched-at, per-item workItemId/scope/taskId, honest "worker/provider not tracked" disclosure, and Abandon-then-prefilled-retry composed from existing real primitives, never a fabricated single-item Retry or a Replan that has no domain primitive); a new persistent, always-visible `GlobalRunStatusIndicator` in `AppShell.tsx`'s sidebar (mission/run-specific entries only, deep-linking to the exact run, real last-checkpoint relative time) closing BUG-14's own disclosed remaining gap from wave 1.

BUG-05 (Health Repair state loss after navigating away) was investigated read-only first: root-caused precisely (Health Repair has zero durable-operation concept -- no operationId/phase/status anywhere, unlike Prepare for Work's real `prepareForWorkOperations` store; each action is one long-lived synchronous request tracked only in transient React `useState`, silently dropped if the component unmounts before it resolves) with an exact reproduction scenario recorded in bug-ledger.json. Deliberately NOT attempted this wave: closing it honestly needs a genuine new durable-operation backend design (a real design decision -- reuse `prepareForWorkOperations`'s shape or build a distinct one -- not a bounded UI-only fix like this mission's other bugs), which deserves its own dedicated wave rather than being rushed in unreviewed alongside BUG-15/visibility.

Both wave-2 features: reproduced/ground-truthed first, unit-tested at every layer touched (domain -> server view -> client), full suite re-run clean, live-verified against the real running app (the sidebar indicator showed a real PAUSED run with its real last-checkpoint relative time, and clicking it deep-linked to the exact `?tab=keep-going&runId=...` URL and rendered that tab active immediately -- zero console errors), then independently verified (separate agent context) with zero defects found on the first pass -- a genuine adversarial hunt for a prefill-after-abandon race and a stale-closure risk in the tick-form prefill, both confirmed correct rather than assumed. One minor coverage nit the verifier found (a missing dedicated test for `lastCheckpointAt` threading one layer up) was closed immediately. A third environmental Windows-full-suite-contention flake (`health-repair-io.test.mjs`'s timeout-kill test, unrelated to this diff) was found and disclosed alongside the other two already-known flakes.

**BUG-17 (approval-consumption) — a genuine judgment call, not resolved autonomously:** ground truth (established during wave 1's BUG-08 investigation) is that chat is structurally zero-tool and cannot execute ANY TIM_REQUIRED action regardless of approval state; the only action class with real execution teeth (Adoption) already has its own safe, separate approval primitives (`domain/operator-action-lifecycle.mjs`) on the Adoption surface, not chat. Building new persisted approval-consumption state for actions chat can't execute anyway would be speculative security-relevant infrastructure with no live capability behind it -- a real risk (a future "shadow authorization" concept someone could misread as meaningful, or wire to something dangerous later without its own review) for no present benefit. Recommend to the operator: either (a) accept this as resolved given the current architecture (chat's consistent, honest refusal is correct behavior, not a bug) and revisit BUG-17 only if/when chat gains real execution capability for a TIM_REQUIRED class, designed together with that capability's own safety review -- or (b) explicitly direct that a minimal mechanism be built now anyway, understanding it currently has no execution path to guard. Not built without that decision.

## Wave 3 (2026-09-03): operator UX cluster, BUG-05 durable operations, BUG-17 decision

Scope: BUG-01 (context returns to Home -- root-caused to the WebView2 desktop launcher, `launcher/Launch-TSF.ps1` + `launcher/first-run-setup.html`, not the React SPA), BUG-02 (Projects nav-position, URL-persisted filter/search/sort), BUG-03 (Work Set/membership confusion across tabs -- 3 distinct root causes), BUG-04 (composer auto-grow), BUG-05 (Health Repair durable-operation state -- a new, deliberately-separate durable-operation system mirroring the already-adopted Prepare for Work pattern), BUG-06 (mission-creation-vs-dispatch clarity), BUG-07 (raw IDs in notifications -- TSF-owned half only, see below), BUG-09 (raw orchestration terminology), BUG-10 (Run Now form labels). Checkpointed across four commits: `67a8d4d483`, `98d2b7d67b`, `1a67a21431`, `7bc6778018`.

**BUG-17 decision (explicit operator instruction, preserved verbatim in intent):** reclassified `DEFERRED / NOT_APPLICABLE_TO_CURRENT_CHAT_CAPABILITY` — do not build persisted scoped-approval-consumption infrastructure while Planner Chat has no real consequential execution capability behind it. Preserved as a MANDATORY prerequisite (scoped one-time consumption, project/action scoping, replay prevention, independent safety review) the moment Planner Chat gains real consequential execution authority. ACCEPT-07 adjusted to `SATISFIED_FOR_CURRENT_CHAT_CAPABILITY` accordingly — not a release blocker for this candidate, not silently marked done either.

Every bug in this wave: root-caused first (a dedicated investigation agent for several), fixed, unit-tested, and put through at least one round of independent verification (separate agent context) — several rounds found and the implementer fixed real defects: two real security bypasses in BUG-01's origin-check logic (a plain string-prefix check, then a base-URL-mismatch bypass found on the round-2 fix itself — three total verification rounds before clean), a real `workSetBefore` staleness race in BUG-03, a real error-masking bug in BUG-05's `runBaseline()` (missed on the first pass, found on a full adversarial re-check), a real accessibility gap in BUG-10 (labels with no `htmlFor`/`id` association). Full narrative per bug in `bug-ledger.json`.

New golden-path e2e test file added: `test/golden-path-operator-flow.test.mjs` (3 tests) — proves cross-surface state agreement (Keep Going/Work/Flight Recorder/Planner Chat) for the same real run through ACTIVE → STALLED → recovered ACTIVE, a genuine NEEDS_YOU escalation, and a real COMPLETE/`readyForAdoption` transition, at the fixture/HTTP level. (Superseded in coverage terms, not replaced, by the final review wave's real-project evidence below.)

## Final Review Wave (2026-09-03): real-project/live-runtime validation

Per an explicit operator instruction to determine whether the Wave-3 candidate could legitimately advance past `READY_FOR_FINAL_REVIEW`. Checkpointed across three commits: `23e9d419ad`, `0b371f5527`, plus the ledger-sync-correction content folded into `23e9d419ad`.

**Real-project methodology:** an isolated scratch TSF server instance was run for this wave (own port `4712`, own state file `server/.local-state/operator-state.real-project-validation.json`, git-ignored, torn down and deleted at the end of the wave) — **never** the live production TSF instance already running on port `4610` on this machine, which was touched only via read-only `GET` reconnaissance (confirmed genuinely real, non-fixture project data: `tsf-orca` STALLED, `niners-war-room` PAUSED/NEEDS_YOU, `landing-page` genuinely COMPLETE/`READY_FOR_ADOPTION`). Real onboarded projects for the scratch instance came from two sources: this repo's own `tsf/pilots/` real-pilot registrations (`colety-labs-sales-engine`, `weird-talent-marketplace`, `shopify-catalog-qa`, real local git repos under `C:/Users/codex-agent/Documents/ChatGPT/Idea Incubator/`) and freshly onboarded siblings from the same directory (`route-reader`, `quoteloop`, `snow-jams`) via the real `POST /api/onboarding/analyze`+`commit` flow (read-only analysis, confirmed by the code's own comment). The underlying task EXECUTOR for every real dispatch was still the safe stub Orca CLI every test in this repo already uses — spawning a genuinely uncontrolled real coding agent was judged out of proportion for an autonomous validation wave without further explicit operator sign-off (disclosed everywhere this matters, see `ACCEPT-01`/`ACCEPT-02` in `bug-ledger.json`).

**What this proved, live, against real (non-fixture) data**, confirmed both via direct HTTP calls and via a real rendered Chrome browser session against the scratch instance's built UI: real Keep Going mission creation via Planner Chat dispatch; global run-status indicator agreement; genuine wall-clock STALLED induction + real recovery via the UI's own Abandon-stalled-wave control, with the complete real Flight Recorder event timeline and the honest "Workers: none dispatched yet" disclosure rendering correctly; Evidence tab showing real (non-empty, non-fabricated) result capsules for both a legacy candidate and a real Keep Going wave outcome; a real project independently reaching COMPLETE on the live production instance (observed read-only, not engineered) with all three surfaces agreeing; composer auto-grow with real typed multi-line content (36px → 96px, correct Shift+Enter-for-newline vs Enter-to-submit behavior); and canonical cross-surface state agreement (Keep Going/Work/Flight Recorder/Planner Chat) throughout a real run's ACTIVE → WORKING → VERIFYING → STALLED → recovered lifecycle.

**Real defects found and fixed during this wave** (each independently re-verified, several through 2-3 rounds):
- **BUG-02's real remaining gap**: the Wave-3 fix (URL-persisted filter/search/sort) never actually closed the bug's own literal original complaint — leaving Projects for an unrelated section (Health Repair) and returning still required manually re-finding/re-scrolling to the project just viewed, since the fix only covered a within-Projects click-in-and-back round trip. Reproduced live against a real project. Fixed with a new `ui/src/lib/last-viewed-project.ts` (localStorage) wired into `ProjectDetailPage.tsx`/`ProjectsPage.tsx` (auto-scroll on a genuinely fresh arrival only). One real regression in the first version of this fix (writing on the raw route param instead of the successfully-loaded project) was found and fixed.
- **A fourth BUG-09-class terminology gap**: the Evidence tab's per-result-capsule status badge rendered raw `SCREAMING_SNAKE_CASE` (e.g. `ABANDONED_STALLED`), missed by Wave 3's original sweep. Fixed with the existing `humanizeConstant` helper.
- **BUG-07's definitive conclusion**: a dedicated read-only investigation of Orca-core source (`src/main`, `src/renderer` — never modified) found the real answer is split. TSF's `displayName` fix genuinely improves the terminal tab title (`agent-row-primary-text.ts`'s own already-wired preference for it). It does **not** and structurally **cannot** improve OS notification content — `NotificationDispatchRequest`'s type contract has no field to carry `displayName` through at all. Status moved to the new, documented `PARTIALLY_READY / ORCA_CORE_DEPENDENCY`. A concrete, scoped future Orca-core change proposal (exact files/lines, behavior, tests, LOW migration risk) is recorded in BUG-07's own ledger entry — not implemented, Orca core untouched.
- **A cross-cutting interaction defect** (found only by a dedicated final holistic review looking across the whole diff, not any single bug's own review): `BulkActionBar`'s `refreshing` gate (BUG-03) closed the original staleness race only for a *successful* background reload — a *failed* one (network blip) left `loading` cleared but `portfolio`/`workSetBefore` stale, reopening the exact fabricated-cascade risk BUG-03 exists to prevent. Fixed: `refreshing={loading || !!error}`, with the existing `RefreshFailedBanner` Retry as the real recovery path.

**What was explicitly NOT attempted, disclosed honestly:**
- **BUG-01's live WebView2 desktop recovery-navigation flow**: a real `msedgewebview2.exe` process is already running on this machine (the live production TSF instance's own WebView2 host) — touching or restarting it, or launching a second instance against the hardcoded `http://127.0.0.1:4610`, would risk interfering with that live instance, explicitly forbidden. Strengthened instead via a genuine Chrome-browser JS-engine execution of the exact `resolveReturnUrl()` logic (same Chromium URL parser WebView2 embeds) against all known bypass patterns — all correctly blocked, the legitimate deep-link case round-trips exactly. The full live desktop-app flow itself remains unexercised; a real, live, manual check on a real machine (not this automated environment) is still the required final step before treating BUG-01 as fully proven end to end.
- **ACCEPT-02 (isolated worktree creation)**: not verified this wave — requires the real (non-stub) Orca CLI to actually spawn a coding agent and provision a real git worktree, deliberately not attempted (same reasoning as the stub-executor disclosure above). Marked `NOT_VERIFIED_THIS_WAVE`, not silently assumed to work.
- **ACCEPT-09 (post-adoption live smoke tests)**: by definition cannot be satisfied before adoption. See "Post-adoption plan" below for the exact list.

**Final full verification bar, repeated multiple times across this wave:** `tsf/ui` — `tsc -b --noEmit` clean, `vite build` clean, `node --experimental-strip-types --test src/**/*.test.ts` 87/87 pass (up from 82 at the start of this wave). `tsf` server suite — 1009/1011 pass, 1 known pre-existing Windows-full-suite-contention flake (`test/keep-going-autonomy-proof.test.mjs`, reconfirmed passing in isolation multiple times this session), 1 skipped — no regressions from this wave's changes.

## Post-adoption plan (prepared, not executed — no adoption happened this session)

**Pre-adoption safety check (real, already-implemented mechanism — do not invent a new one):** `GET /api/update-safety` (`server/safe-update-http-routes.mjs`, backed by `domain/update-safety.mjs`'s `classifyUpdateSafety`) against the live instance, gated on `feed.executing` (the one real, unambiguous "would restarting interrupt this run right now" fact) — must read `SAFE_NOW` before proceeding, or `TIM_REQUIRED`/`WAIT_FOR_ACTIVE_WORK` blocks adoption until resolved. As of this session's own read-only snapshot of the live instance, at least `tsf-orca` (STALLED, not executing) and `niners-war-room` (PAUSED with an open Needs You question, not executing) exist — re-check with a fresh `GET /api/update-safety` call immediately before any real adoption attempt, since this snapshot will be stale by the time anyone acts on it.

**Adoption sequence (this repo's own established governance, `domain/self-update-adoption.mjs`):** `assessAdoptionReadiness({candidateIsFastForward, candidateWorktreeClean, liveMainClean, independentReviewGreen, requiredTestsGreen, orcaCoreDeltaZero})` — every gate is already satisfiable from this session's own evidence (fast-forward confirmed via `git merge-base --is-ancestor tsf/main HEAD`, worktree clean, independent review green per `bug-ledger.json`'s per-bug history, tests green per the verification bar above, Orca-core delta confirmed zero via `git diff --stat tsf/main..HEAD -- src/` returning nothing). Only Tim can author `createAdoptionReceipt` (`decidedBy !== 'TIM'` throws by design) — a real, local-only fast-forward merge of `tsf/main` to this candidate's HEAD, followed by a reload/restart of **only the TSF plugin-host Node process** (`server/http-server.mjs`'s `startStandaloneServer`, or `main.mjs`'s `activate()` when embedded as an Orca plugin) — never Orca core, never the whole desktop app.

**Post-adoption smoke tests (real, already-implemented mechanism):** `verifyLiveRuntime()` (`server/post-update-verification.mjs`) against the freshly-restarted instance — 7 real HTTP checks (`api-health`, `runtime-identity`, `projects-load`, `work-load`, `capacity-load`, `orca-integration-status`, `planner-chat-basic-call`), plus this mission's own specific golden paths against a REAL (not fixture) project: Evidence wiring shows real results; a Work/Home card's deep link opens the exact tab/run; canonical state agreement holds across Work/Keep Going/Flight Recorder/Planner for a real in-flight run; a real STALLED run (or a freshly, safely induced one) recovers correctly via Abandon; Health Repair survives a navigate-away-and-back; Projects auto-scrolls back to the last-viewed project after a cross-section detour; the terminal tab title for a freshly-dispatched real task shows a human-readable label (confirming BUG-07's real, partial effect) while accepting that OS notification content will *not* improve (expected, disclosed, not a failure).

**Rollback (real, local-only, no data loss):** fast-forward `tsf/main` back to its pre-adoption SHA (`2613be403cd12ed27f2953e9fade6521ebb0fb79`) and restart the TSF process. Safe: no persisted state schema was changed in a backward-incompatible way — BUG-05's `opState.healthRepairOperations` is a new, purely-additive top-level field (already in `DEFAULTS`) that older code simply never reads/writes, and every other change in this diff is UI/route/domain logic operating on the existing Keep Going run shape, not a schema change to it.

**Rollback trigger:** any `verifyLiveRuntime()` check failing, or any post-adoption smoke test above failing, or an operator judgment call that something real and unexpected happened.

## Operating rules

- Fresh isolated worktrees for implementation, based on current accepted `tsf/main`.
- Reproduce each bug before fixing it.
- Independent verification (separate identity/context) before READY_FOR_ADOPTION.
- Zero Orca-core delta.
- Stop candidates at READY_FOR_ADOPTION — never auto-merge/push/deploy/adopt, never restart live TSF, never mutate NWR.
- Routine technical decisions made autonomously. NEEDS_YOU surfaced only for genuinely consequential authority, unrecoverable ambiguity, or governed adoption.
